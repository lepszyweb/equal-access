---
title: "Pomoc dla reguły: HAAC_Combobox_DOM_Focus"
---
import "../../../styles/ToolHelp.scss"
import { CodeSnippet, Tag } from "carbon-components-react";

<div className="toolHelp">
<Row>
<Column colLg={16} colMd={8} colSm={4} className="toolHead">

### Początkowy fokus DOM nie jest poprawnie ustawiony w polu combobox

<div id="locLevel"></div>

Początkowy fokus DOM w combobox musi być ustawiony w polu tekstowym

</Column>
</Row>
<Row>
<Column colLg={11} colMd={5} colSm={4} className="toolMain">

### Dlaczego to jest ważne

Pole kombinowane (combobox) to interaktywny komponent UI, który łączy dane wejściowe i inny element, taki jak pole listy lub siatka, który może dynamicznie wyskakiwać, aby pomóc użytkownikowi ustawić wartość wprowadzanych danych. Gdy combobox otrzymuje fokus, powinien on być ustawiony na elemencie wejściowym `<text>`, który może albo wyświetlać aktualną wartość albo obsługiwać edycję, tak aby osoby korzystające z technologii wspomagających mogły precyzyjnie nawigować i wchodzić w interakcję z treścią.

<div id="locSnippet"></div>

### Co zrobić

* Ustaw początkowy fokus DOM tylko na elemencie comboboxa `<text>` przyjmującym dane. 

(Uwaga: Było to konieczne w przypadku `combobox` wykorzystującego ARIA 1.1, czego należy unikać ze względu na problemy z implementacją. Należy stosować się do wskazówek dotyczących stosowania combobox zawartych w [ARIA 1.2](https://www.w3.org/TR/wai-aria-1.2/#combobox), które uległy znacznej zmianie).

**Uwaga**: Wytyczne dla comboboxa zmieniły się znacząco w WAI-ARIA 1.2 z powodu problemów z implementacją wzorca WAI-ARIA 1.1. Rekomendowanym wzorcem comboboxa dla WAI-ARIA 1.2 jest:

</Column>
<Column colLg={5} colMd={3} colSm={4} className="toolLeft">

### O tym wymaganiu

* [KS 4.1.2 Nazwa, rola, wartość](https://wcag.lepszyweb.pl/#name-role-value)
* [WAI-ARIA v1.1 Authoring Practices - Combobox](https://www.w3.org/TR/wai-aria-practices-1.1/#combobox)
* [WAI-ARIA v1.2 Authoring Practices - Combobox](https://www.w3.org/TR/wai-aria-practices-1.2/#combobox)

### Na kogo to wpływa

* Osoby niewidome korzystające z czytników ekranu
* Osoby słabowidzące korzystające z czytników ekranu

</Column>
</Row>
</div>

export default ({ children, _frontmatter }) => (<React.Fragment>{children}</React.Fragment>)
